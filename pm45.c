#include<stdio.h>
int main(void){
    int a[3],i,n,count=0;
    printf("\nenter the numbers:");
    for(i=0;i<n;i++)
    {
    scanf("%d",&a[i]);
    count++;
    }
    printf("\nThe number of numeric digits is:%d",count);
    return 0;
}
